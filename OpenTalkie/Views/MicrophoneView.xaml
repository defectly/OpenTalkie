<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="OpenTalkie.Views.MicrophoneView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:OpenTalkie.Views"
    xmlns:vm="clr-namespace:OpenTalkie.ViewModels"
    Title="MicrophoneView">

    <!--<ContentPage.BindingContext>
        <vm:MicrophoneViewModel />
    </ContentPage.BindingContext>-->

    <ListView HasUnevenRows="True" ItemsSource="{Binding Settings}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <views:SettingView BindingContext="{Binding .}" />
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
    <!--<VerticalStackLayout>
        <local:CardView BindingContext="{Binding SampleRate}" />
        <local:CardView BindingContext="{Binding ChannelType}" />
        <local:CardView BindingContext="{Binding MicrophoneType}" />
    </VerticalStackLayout>-->
</ContentPage>
