<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="OpenTalkie.Presentation.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:OpenTalkie.Presentation.Views"
    xmlns:viewmodel="clr-namespace:OpenTalkie.Presentation.ViewModels;assembly=OpenTalkie.Presentation"
    x:Name="SettingsPageRoot"
    x:DataType="viewmodel:SettingsViewModel"
    Padding="16,24,16,16">
    <CollectionView
        Margin="0,8,0,0"
        ItemsSource="{Binding SettingsItems}">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="viewmodel:SettingsItem">
                <Border
                    Style="{StaticResource M3CardBorder}"
                    Margin="0,8"
                    Padding="16"
                    StrokeShape="RoundRectangle 8">
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding Source={x:Reference SettingsPageRoot}, Path=NavigateToSettingsPageCommand, x:DataType=views:SettingsPage}" CommandParameter="{Binding .}" />
                    </Border.GestureRecognizers>
                    <Label
                        FontAttributes="Bold"
                        FontSize="16"
                        Text="{Binding Name}"
                        TextColor="{AppThemeBinding Light={StaticResource OnSurfaceLight}, Dark={StaticResource OnSurfaceDark}}" />
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>


